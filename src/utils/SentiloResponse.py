def success(code, message):
    message = {
        'code' : code,
        'message' : message
    }
    return message
    
def error(code, message):
    message = {
        'code' : code,
        'message' : message
    }
    return message
